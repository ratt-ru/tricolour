language: python
matrix:
  include:
  - env: TARGET=python2
  - env: TARGET=python36
  - env: TARGET=pep8
sudo: required
services:
- docker
before_install:
- true
install:
- docker build . -t ${TARGET} -f .travis/${TARGET}.docker
before_script:
- true
script:
- true
